import React from "react";
import { InventoryItem } from "@/types/inventory";

interface UnlockedItemProps {
  item: InventoryItem;
}

const UnlockedItem: React.FC<UnlockedItemProps> = () => <></>;

export default UnlockedItem;
