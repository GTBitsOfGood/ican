export enum Provider {
  GOOGLE = "google",
  PASSWORD = "password",
}

export enum ChildPasswordType {
  NORMAL = "normal",
  COLOR = "color",
}

export enum LoginType {
  PARENT = "parent",
  CHILD = "child",
}
